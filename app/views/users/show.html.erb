<%#<div class="col-sm-12">
  <ul class="nav nav-tabs nav-justified mb-3 nav-pills">
    <li class="nav-item">
      <a href="#posts-list" class="nav-link active" data-toggle="tab">投稿記事一覧</a>
    </li>
    <li class="nav-item">
      <a href="#ikitai-list" class="nav-link" data-toggle="tab">行きたいね</a>  
    </li>
  </ul>
</div>%>

<ul class="nav nav-tabs nav-justified mb-3 nav-pills">
  <li class="nav-item">
    <a href="<%= user_path(@user) %>" class="nav-link <%= 'active' if current_page?(user_path(@user)) %>">投稿記事一覧<span class="badge badge-secondary"></span></a>
  </li>
  <li class="nav-item">
    <a href="<%= ikitais_user_path(@user) %>" class="nav-link <%= 'active' if current_page?(ikitais_user_path(@user)) %>">💛行きたいね<span class="badge badge-secondary"></span></a>
  </li>
</ul>

<div class="tab-content">
  <div id="posts-list" class="tab-pane active">
    <% if @posts.any? %>
      <%= render 'users_posts', posts: @posts %>
    <% end %>
  </div>
</div>