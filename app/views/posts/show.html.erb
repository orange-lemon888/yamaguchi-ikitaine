<div class="container">
  <div class="row">
    <div class="col-md-6">

      <p><%= @post.title %></p>

      <% if @post.image? %>
        <%= image_tag @post.image.url,  size: '480x320' %>
      <% end %>

      <P>
        <span class="badge badge-warning">投稿者</span><%= @post.user.name %> 
        <span class="badge badge-warning">ｴﾘｱ</span><%= @post.area %> 
        <span class="badge badge-warning">投稿日</span><%= @post.created_at %>
      </P>
　　
      <p><span class="badge badge-warning">ポイント</span><%= @post.subtitle %></p>
    </div>
    
    <div class="col-md-6">
      <div class="col-md-6">💛行きたいね　件
      
        <% if logged_in? %>
          <% if current_user == @post.user %>
            <%= link_to '編集', edit_post_path(@post) %>
            <%= link_to '削除', @post, method: :delete, data: { confirm: 'Are you sure?' } %>
          <% end %>
        <% end %>
      </div>
      <span class="badge badge-success">投稿内容</span>
      <p><%= @post.content %></p>
    </div>
  </div>
</div>